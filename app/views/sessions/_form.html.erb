<%= form_for(:session, url: login_path) do |f| %>
    <% if flash[:error] %>
      <div class="alert alert-danger">
        <%= flash[:error] %>
      </div>
    <% end %>
    <div class="input-field">
      <%= f.label "Email" %>
      <%= f.email_field :email, class: "validate", required: true %>
    </div>
    <div class="input-field">
      <%= f.label :password %>
      <%= f.password_field :password, class: "validate", required: true %>
    </div>
    <div class="input-field">
      <%= f.check_box :remember_me, class: "check_box", checked: true %>
      <%= f.label :remember_me, "Stay Logged In" %>
    </div>
    <br>
    <div class="input-field">
      <%= f.submit 'Log in', class: 'btn blue' %>
      <%= link_to 'Reset Password', new_password_reset_path, class: "btn orange" %></span>
    </div>
<% end %>
