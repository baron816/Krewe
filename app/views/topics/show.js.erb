$('<%= j render @topic.messages.reverse %>').hide().prependTo($('.individual-messages')).fadeIn(1500)
<% if @topic.next_page %>
  $('.more-messages').replaceWith('<%= j link_to_next_page @topic.messages, "See More", params: {controller: "topics", action: "show", id: @topic.id}, remote: true, class: "btn blue more-messages"  %>')
<% else %>
  $('.more-messages').remove();
<% end %>
