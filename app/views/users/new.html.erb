<%= form_for(@user) do |f| %>
	<div class="col-md-9 col-md-offset-1">
		<% if @user.errors.any? %>
			<% @user.errors.full_messages.each do |msg| %>
				<div class="alert alert-danger">	<%= msg %> </div>
			<% end %>
		<% end %>
		<div class="form-group">
			<%= f.text_field :name, placeholder: "Name", class: 'form-control', required: true %>
			<%= f.email_field :email, placeholder: 'Email', class: 'form-control', required: true %>
		</div>

		<div class="form-group", id="address-info">
			<%= f.text_field :street, placeholder: 'Street Address', class: 'form-control', required: true %>
				<%= f.text_field :city, placeholder: "City", class: 'form-control', required: true %>
				<%= f.select :state, us_states, {}, class: 'form-control', required: true %>
		</div>

		<%= f.hidden_field :latitude, id: "lat" %>
		<%= f.hidden_field :longitude, id: 'lng' %>
		<% categories.each do |category| %>
			<div class="input-group">
			<span class="input-group-addon"> <%= f.radio_button :category, category %></span>
			<%= f.label :category, category, class: 'form-control', required: true %>
			</div>
		<% end %>
		<%= f.password_field :password, placeholder: 'Password', class: "form-control", required: true %>
		<%= f.password_field :password_confirmation, placeholder: 'Confirm Password', class: "form-control", required: true %>
		<%= f.submit :Submit, class: 'btn btn-primary form-control' %>
	</div>
<% end %>
