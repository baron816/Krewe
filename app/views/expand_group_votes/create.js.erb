$('#expand').replaceWith('<%= link_to "Don\'t expand #{@group.expand_group_votes_size}/#{@group.users_count}", group_expand_group_vote_path(@group, @expand_vote), method: :delete, class: 'btn btn-danger', id: 'remain', remote: true %>')
