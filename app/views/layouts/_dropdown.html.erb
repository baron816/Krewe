<% if current_user.active_notifications.any? %>
	<a href="#" class="dropdown-toggle" data-toggle='dropdown' role='button' aria-expanded='false'>Notifications <span class='caret'></span></a>
	<ul class="dropdown-menu" role='menu'>
		<% current_user.active_notifications("Message").each do |notification| %>
			<li><a href="/groups/<%= notification.group.id %>"><%= notification.poster_name %> wrote in <%= notification.group_name %> <%= local_time_ago(notification.created_at) %></a></li>
		<% end %>
		
		<% current_user.active_notifications("Personal").each do |notification| %>
			<li><a href="/users/<%= notification.poster.id %>/public_profile "><%= notification.poster_name %> sent you a message <%= local_time_ago(notification.created_at) %></a></li>
		<% end %>
		
		<% if current_user.render_divider? %>
			<li class="divider"></li>
		<% end %>
		
		<% current_user.active_notifications("Join").each do |notification| %>
			<li><a href="/groups/<%= notification.group.id %>"><%= notification.poster_name %> joined <%= notification.group_name %></a></li>
		<% end %>

	</ul>
<% end %>
