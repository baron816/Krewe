<nav class="navbar navbar-default">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#right-section" aria-expanded="false">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>

			<a href="/" class="navbar-brand">
				<span class="logo">Sidereal</span>
			</a>
		</div>
		<div class="navbar-collapse collapse" id="right-section">
			<ul class="nav navbar-nav navbar-right">
				<% if current_user %>
					<li> <%= link_to current_user.name, user_path(current_user)%></li>
					<li class="dropdown"><%= render 'layouts/dropdown' %></li>
					<li> <%=  link_to 'Log Out', logout_path, method: :delete %></li>

				<% else %>
					<li><%= link_to "Log In", login_path, remote: true%></li>
					<li><%= link_to "Sign Up", new_user_path %></li>
				<% end %>
			</ul>
		</div>
	</div>
</nav>
