$('<%= j render partial: "show", locals: {message: @message} %>').prependTo('.messages').hide().slideDown("slow")

if ($('.messages > div').length >= 5){
	$('.messages div:last').remove()
}

$('#message_content').val('')