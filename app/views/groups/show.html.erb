<h1>
	<%= @group.name %>
</h1>

<h2>Members</h2>
<ul>
	<% @group.users.each do |user| %>
		<li><%= link_to user.name, user_path(user) %></li>
	<% end %>
</ul>

<h2>Messages</h2>

<% @group.messages.each do |message| %>
	<div class="well">	
		<%= message.user.name %> at <%= message.created_at %>	: <br>
		<%= message.content %> 
	</div>
<% end %>


<h2>Write a new message</h2>
<%= render "messages/new" %>