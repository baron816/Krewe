$('li.next-topic').replaceWith('<%= j render partial: "topics/topics", locals: { topics: @group_show.topics}  %>')
<% unless @group_show.next_page %>
  $('.next-topic').remove();
<% end %>
highlightTopic()
