$('.next-topic').replaceWith('<%= j render partial: "topics/topics", locals: { topics: @group_show.topics}  %>')
<% unless @group_show.next_page %>
  $('.next-topic').remove();
<% end %>

$('.topics').animate({ scrollLeft: $(this).width() }, 1000)
highlightTopic()
